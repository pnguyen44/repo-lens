[project]
name = "repo-lens"
version = "0.1.0"
description = "AI-powered CLI for chatting about GitHub conversations"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.104.1",
    "chainlit>=2.11.1",
    "chromadb",
    "google-genai>=2.3.0",
    "mcp>=1.27.1",
    "numpy>=2.4.6",
    "python-dotenv",
    "qdrant-client>=1.18.0",
    "rich",
    "structlog>=26.1.0",
    "voyageai>=0.3.7",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "mypy"]

[project.scripts]
repo-lens = "repo_lens.app.chat_cli:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/repo_lens"]

[tool.pytest.ini_options]
pythonpath = ["src"]
asyncio_mode = "auto"

[tool.pyright]
venvPath = "."
venv = ".venv"
extraPaths = ["./src"]
typeCheckingMode = "off"

[tool.mypy]
strict = true
files = ["src", "tests"]
explicit_package_bases = true
mypy_path = "src"

# Skip type-checking inside the anthropic package to avoid a mypy internal crash on Python 3.14
[[tool.mypy.overrides]]
module = "anthropic.*"
follow_imports = "skip"

# Relax strict rules for tests — catch real type errors but don't nag about annotations
[[tool.mypy.overrides]]
module = "tests.*"
disallow_untyped_defs = false
disallow_incomplete_defs = false
disallow_untyped_decorators = false

# Chainlit has no usable type stubs
[[tool.mypy.overrides]]
module = "chainlit.*"
ignore_missing_imports = true
